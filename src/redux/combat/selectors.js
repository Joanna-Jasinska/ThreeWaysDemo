// export const selectError = (state) => state.combat.error;
export const selectLoading = (state) => state.combat.isLoading;
export const selectTimeline = (state) => state.combat.timeline;
export const selectTicks = (state) => state.combat.ticks;
export const selectPause = (state) => state.combat.pause;
export const selectEndCombat = (state) => state.combat.endCombat;
